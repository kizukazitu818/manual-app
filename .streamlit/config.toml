[server]
maxUploadSize = 1024

[theme]
base = "light"
primaryColor = "#007BFF"        # ボタンなどのテックブルー（濃いめ）
backgroundColor = "#FFFFFF"     # メイン背景（白）
secondaryBackgroundColor = "#E6F3FF" # ★ここ！サイドバー（淡いテックブルー）
textColor = "#2C3E50"           # 文字色（ダークネイビー）
font = "sans serif"
